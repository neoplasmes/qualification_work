/**
 * there should be bundle size optimization applied after lazy loading of
 * exactly this export.
 *
 * @see https://motion.dev/docs/react-reduce-bundle-size
 */
export { domMax } from 'motion/react';
