import type {
    ActionComputedOperation,
    ActionEffect,
    ActionParameterType,
    ActionValueOperation,
} from '@/entities/action';

export type ActionParameterDraft = {
    id: string;
    key: string;
    label: string;
    type: ActionParameterType;
    required: boolean;
    defaultValue: string;
    hidden: boolean;
    keyAutoGenerated: boolean;
};

export type ActionValueMappingDraft = {
    id: string;
    columnKey: string;
    sourceKind: 'parameter' | 'literal' | 'computed';
    parameterKey: string;
    literalValue: string;
    operation: ActionValueOperation;
    leftParameterKey: string;
    computedOperation: ActionComputedOperation;
    rightParameterKey: string;
};

export type ActionEffectDraft = {
    id: string;
    kind: ActionEffect['kind'];
    datasetId: string;
    matchColumnKey: string;
    matchSourceKind: 'parameter' | 'literal';
    matchParameterKey: string;
    matchLiteralValue: string;
    maxRows: string;
    values: ActionValueMappingDraft[];
};

export type ActionDraft = {
    id: string | null;
    name: string;
    description: string | null;
    parameters: ActionParameterDraft[];
    effects: ActionEffectDraft[];
};

export {
    actionToDraft,
    coerceRunValues,
    createBlankActionDraft,
    createBlankEffectDraft,
    createBlankParameterDraft,
    createBlankValueMappingDraft,
    draftToActionPayload,
    getDefaultRunValues,
    labelToKey,
} from '../lib';
