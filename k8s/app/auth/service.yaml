apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: app
spec:
  # headless so the gateway discovery sidecar can resolve pod ips directly
  clusterIP: None
  selector:
    app: auth
  ports:
    - port: 3002
      targetPort: 3002
