apiVersion: v1
kind: Service
metadata:
  name: data-service
  namespace: app
spec:
  # headless so the gateway discovery sidecar can resolve pod ips directly
  clusterIP: None
  selector:
    app: data-service
  ports:
    - port: 3003
      targetPort: 3003
