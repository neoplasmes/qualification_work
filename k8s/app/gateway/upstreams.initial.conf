#* this file called initial because it's just an initializer and rael upstreams config will
#* be served by openresty-discovery sidecar. however in e2e there will be another approach, 
#* because there will be used docker-compose (see e2e folder)

upstream auth_backend {
    least_conn;
    server 127.0.0.1:65535 down;
}

upstream client_backend {
    least_conn;
    server 127.0.0.1:65535 down;
}

upstream server_backend {
    least_conn;
    server 127.0.0.1:65535 down;
}

upstream data_service_backend {
    least_conn;
    server 127.0.0.1:65535 down;
}
